
    <div class="container" style="margin-top: 65px">
      <div class="row main col-md-6">
        <div class="panel-heading" >
                 <div class="panel-title text-center">
                    <h1 class="title">Login</h1>
                    <hr />
                  </div>
              </div>

        {{#if errors}}
          {{#each errors}}
          <div class="alert alert-danger">{{msg}}</div>
          {{/each}}
        {{/if}}

  <!-- Main Form -->
  <div class="login-form-1">
    <form id="login-form" class="text-left" method="post" action="/local/login">
      <div class="login-form-main-message"></div>
      <div class="main-login-form">
        <div class="login-group">
          <div class="form-group">
            <label for="lg_username" class="sr-only">Username</label>
            <input type="text" class="form-control" id="username" name="username" placeholder="username">
          </div>
          <div class="form-group">
            <label for="lg_password" class="sr-only">Password</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="password">
          </div>

        <button type="submit" class="btn btn-primary btn-lg outline" >Login</button>

      </div>

      </div>
    </form>
  </div>
  <!-- end:Main Form -->


</div>


    <div class="container col-md-6">

    <div>

{{!--       <a type="button" href="/local/register" class="btn btn-primary btn-lg outline" style="margin-top: 20px" id="needToRegister">Whoops! I don't have and account and need to register!</a> --}}

      <span class="appNameLogin animated fadeInDown" style="margin-left: 25px">The LowDwn.</span>


{{!--
      <span class="appNameTwoLogin animated fadeOutUp" style="margin-left: 25px">The LowDwn.</span>

      <span class="appNameThreeLogin animated fadeOutUp" style="margin-left: 25px">The LowDwn.</span>

      <span class="appNameFourLogin animated fadeOutUp" style="margin-left: 25px">The LowDwn.</span>

      <span class="appNameFiveLogin animated fadeOutUp" style="margin-left: 25px">The LowDwn.</span> --}}

    </div>

    </div>

    </div>
