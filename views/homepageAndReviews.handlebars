
        {{#if errors}}
          {{#each errors}}
          <div class="alert alert-danger">{{msg}}</div>
          {{/each}}
          {{/if}}

<div class="card col-md-6">
  <img class="card-img-top" src={{user.image}} alt="Card image cap" style="margin-top: 100px">
  <div class="card-block" data-id={{user.id}}>
    <h2 class="card-title insert-username">Hi {{user.name}}!</h2>
<!--     <p class="card-text">
      Some quick example text to build on the card title
      and make up the bulk of the card's content.
    </p> -->
  </div>

     <h1 style="" class="searchResults" style="margin-bottom: 20px">My Reviews</h1>

{{#each review}}
{{!-- {{#if this._id}}
 --}}
        <div class="" id="daterDiv" style="padding: 30px; text-align: center">

            <img class="card-img-top" src="{{this.review.image}}">

              <div class="card-block">

                <p style="font-size: 15px" class="card-title">{{this.review.username}}</p>
                <p style="font-size: 15px" class="card-title">{{this.review.score}}</p>
                <p style="font-size: 15px" class="card-title">{{this.review.nicety}}</p>
{{!--               <a data-id="{{this._id}}" class="btn btn-primary">Write Review!</a>
 --}}         </div>

        </div>
  {{!-- {{/if}} --}}
 {{/each}}


    {{!-- </div> --}}

</div>

{{!-- adding in a temp div just to see if stories work and no page set up yet --}}
  <div class="container col-md-6" style="margin-top: 65px">
     <h1 style="" class="storyResults">The Latest Stories on TheLowDwn.</h1>

        {{#each story}}
          <div class="flip-container" ontouchstart="this.classList.toggle('hover');">
              <div class="flipper">
                <div class="front">
                  <h2>{{this.title}}</h2>
                  <h4>Category is...{{this.category}}</h4>
                  <span>
                </div>
                <div class="back">
                  <p>{{this.story}}
                </div>
            </div>
        </div>

{{!--         <div>
          <h2>{{this.title}}</h2>
          <h4>Category is...{{this.category}}</h4>
          <div>
          </div>
        </div> --}}


         {{/each}}
    </div>


{{!-- end of temp div for testing dating stories --}}










